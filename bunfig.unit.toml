# Unit test configuration: pure src/lib behaviour only. No containers, so this
# is the fast default path (`bun test`). Each suite imports `should` directly,
# so no preload is needed.
[test]
root = "tests/unit"
coverageReporter = ["text", "lcov"]
coverageDir = "coverage/unit"
coverageSkipTestFiles = true
